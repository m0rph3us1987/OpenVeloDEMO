{
  "name": "openvelo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck -w @openvelo/types && npm run typecheck -w @openvelo/api && npm run typecheck -w @openvelo/web",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @openvelo/api\" \"npm run dev -w @openvelo/web\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}